{
  "name": "InsecureConnector",
  "config": { 
    "connector.class": "io.confluent.connect.jdbc.JdbcSinkConnector",
    "topics": "dice-rolls",
    "tasks.max": "1",
    "connection.url": "jdbc:postgresql://postgres:5432/test",
    "connection.user": "test",
    "connection.password": "test",
    "auto.create": true,
    "auto.evolve": true

  }
}
